<form>
  <h2>Contact Us</h2>

  {{input-for name}}
  {{input-for email type="email"}}
  {{input-for subject}}
  {{input-for message type="text"}}

  <button {{action "submit"}}>Submit</button>
</form>